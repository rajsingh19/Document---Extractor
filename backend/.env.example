# OpenAI API Key for structured extraction (Optional: system runs with heuristic fallback if not set)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Database URL
DATABASE_URL=sqlite:///./senseible_documents.db

# Storage
UPLOAD_DIR=uploads

# Server Port
PORT=8005
HOST=0.0.0.0
